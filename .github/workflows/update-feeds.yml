name: Update All Feeds Data

on:
  schedule:
    - cron: '0 23 * * *' # UTC 23:00 (北京 07:00)
    - cron: '0 3 * * *'  # UTC 03:00 (北京 11:00)
    - cron: '0 7 * * *'  # UTC 07:00 (北京 15:00)
    - cron: '0 11 * * *' # UTC 11:00 (北京 19:00)
  workflow_dispatch:

jobs:
  update-feeds:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -r scripts/requirements.txt

      - name: Run all scrapers
        run: |
          python scripts/fetch_all.py all

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add feeds/
          # 只有当有变更时才提交
          git diff --quiet && git diff --staged --quiet || git commit -m "Update all feeds data [skip ci]"
          git push
